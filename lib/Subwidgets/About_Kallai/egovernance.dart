import 'package:flutter/material.dart';

class Egovernance extends StatelessWidget {
  const Egovernance({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Egovernance"),
    );
  }
}
