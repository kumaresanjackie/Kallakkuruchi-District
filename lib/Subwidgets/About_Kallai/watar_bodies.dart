import 'package:flutter/material.dart';

class WaterBodies extends StatelessWidget {
  const WaterBodies({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Water Bodies"),
    );
  }
}
